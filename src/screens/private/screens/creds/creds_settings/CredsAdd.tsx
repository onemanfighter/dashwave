/**
 * Props for the CredsAdd component.
 */
export interface ICredsAddProps {}

/**
 * Component definition for the creds add component.
 * @param props The props for the creds add component.
 * @returns The creds add component.
 */
export default function CredsAdd(props: ICredsAddProps) {
  return <div>Creds Add</div>;
}
